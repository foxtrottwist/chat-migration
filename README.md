# Chat Migration

Capture conversation context for seamless continuation in a new chat.

## Usage

Invoke via Claude Code:

- "save context"
- "migrate chat"
- "export conversation"
- "hitting context limit"

Generates a structured handoff document with decisions, code context, and continuation guidance.
